'use client'

export default function UploadClient() {
  return (
    <div style={{ padding: 40, color: '#fff' }}>
      <h1 style={{ fontSize: 28, fontWeight: 800 }}>/admin/upload</h1>
      <p style={{ opacity: 0.85, marginTop: 10 }}>
        기존 /admin/upload UI/로직을 여기로 옮겨 넣으면 돼.
      </p>
    </div>
  )
}
